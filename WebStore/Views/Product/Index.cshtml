@model WebStore.Models.ViewModel.Products.ProductsVm

<div id="container">
    <h4 style="color:brown;text-align:center;"> <b>&nbsp; &nbsp; @Model.CategoryName </b></h4>
</div>


<hr />

    <form asp-action="Index" asp-controller="Product">
        <input type="hidden" name="categoryId" value="@Model.CategoryId" />
        <input type="text" name="priceGoods" placeholder=" Пошук по ціні" />
        <input type="text" name="name" placeholder=" Пошук по назві  " />
        <button type="submit"> Пошук </button>
    </form>

<hr />

<div id="container">
    @foreach (var pr in Model.Products)
    {
        <div class="card">
            <a href="@Url.Action("About", "Product", new { Id = pr.Id })">
                <img src="@pr.Image" alt="Avatar" style="width:100%">
                <h5><b>@pr.Name</b></h5>
            </a>
            <div class="container">
                <h4><b>@pr.PriceGoods &nbsp;грн</b></h4>
                <hr />
                <p>@pr.Discription</p>
            </div>

        </div>
    }
</div>

@*<div id="container">
        <ul>
            @foreach (var pr in Model)
            {
                <li id="card" >

                        <a href="@Url.Action("About", "Product", new { Id = pr.Id })">
                            <img style="width: 100%;" src="@pr.Image" />
                            <b>@pr.Name</b>
                        </a>
                        <br />
                        <span>@pr.PriceGoods &nbsp;грн</span>
                        <br />
                    <hr />
                        <span >@pr.Discription</span>


                </li>


            }
        </ul>

    </div>*@



<style>

    /*#card {
                    margin: 0;
                    padding: 0;
                }

                #card {
                    display: inline-block;
                    text-align: center;
                    width: 30%;*/ /* задаем размер пункта можно в пикселях*/
    /*padding: 3px;
                    font-size: 100%;
                    margin-right: 15px;*/ /* добавляем отступы между пунктами */
    /*margin-left: 10px;
                    margin-bottom: 10px;
                }

                .border {
                    display: none;
                    margin: 0;
                    padding: 0;
                    border: 1px solid #ddd;
                    border-radius: 4px;
                    padding: 25px;
                }*/

    .card {
        box-shadow: 0 4px 8px 0 rgba(0,0,0,0.2);
        transition: 0.3s;
        width: 30%;
        display: inline-block;
        text-align: center;
        margin-right: 15px;
        margin-right: 15px;
        /*margin-left: 10px;*/
        margin-bottom: 10px;
    }

        .card:hover {
            box-shadow: 0 8px 16px 0 rgba(0,0,0,0.2);
        }

    .container {
        padding: 2px 16px;
    }

    #container {
        background-color: aliceblue;
        border: 1px solid #ddd;
        border-radius: 4px;
        padding: 25px;
    }
</style>
